/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['picsum.photos', 'upload.wikimedia.org', 'commondatastorage.googleapis.com'],
    }
};

export default nextConfig;

